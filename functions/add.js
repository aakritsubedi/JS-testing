/**
 * A function that return the sum of two no.
 * @param {number} num1 First No
 * @param {number} num2 Second No 
 * @returns {number} the sum of first and second no. 
 */

const add = (num1, num2) => {
  return Number(num1) + Number(num2);
};

module.exports = {
  add
}